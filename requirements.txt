# Núcleo (parser/modelo/registro/validación/persistencia): solo stdlib.
# La interfaz web necesita:
nicegui>=3.0
# El generador de código necesita:
jinja2>=3.0

# Para las pruebas end-to-end de la web (opcional, solo desarrollo):
#   pip install playwright && playwright install chromium
